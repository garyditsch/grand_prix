import template from './page_footer.html';

import PageFooterController from './page_footer.controllers';

const pageFooterComponent = {
    template,
    bindings: {
    },
    controller: PageFooterController,
    controllerAs: 'pageHeaderCtrl',
};

export default pageFooterComponent;
